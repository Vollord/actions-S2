name: Demo_act
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo  HelloGH!
      - name: Print a few lines
        run: |
          echo first line
          echo second line
